{
  "name": "redux-rtc",
  "version": "1.0.0",
  "description": "State integration and dispatchers across connectyed clients",
  "main": "./src/index.js",
  "scripts": {
    "build": "rm -Rf ./dist && mkdir ./dist && browserify ./src/index.js --s ReduxRTC > ./dist/index.js",
    "start": "npm run build && http-server -p 8000 -c-1",
    "test": "jest ./src"
  },
  "keywords": [
    "redux",
    "react",
    "WebRTC"
  ],
  "author": "Ryan P. Hansen",
  "license": "ISC",
  "dependencies": {
    "redux": "^3.6.0",
    "rp-utils": "^1.1.0",
    "whatwg-fetch": "^2.0.3"
  }
}
